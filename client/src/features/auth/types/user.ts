export type User = {
  id: number;
  username: string;
  email: string;
  name: string;
  role: string;
  avatarUrl?: string;
  nativeLanguage: string;
  targetLanguage: string;
  createdAt: string;
  updatedAt: string;
};
